Resolved. Cause: Intune PowerShell script `Map-FinBridgeDrives.ps1` was executing as SYSTEM account before the Workstation service (LanmanWorkstation) had started, preventing UNC path resolution to `\\finbridge-fs01\Finance` — a latent defect introduced when the script was migrated from a GPO logon script (USER context) to Intune without updating the execution context setting. Action: Intune script property "Run this script using the logged-on credentials" changed from No (SYSTEM) to Yes (logged-on user); Finance device group sync triggered; drives mapped successfully on next login. Preventive: Intune script deployment checklist to be updated to enforce "Run as logged-on user: Yes" as a mandatory gate for any script accessing UNC paths or network resources; all existing SYSTEM-context Intune scripts referencing UNC paths to be audited within 10 business days. User confirmed working.
